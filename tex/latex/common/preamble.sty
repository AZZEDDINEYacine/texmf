\ProvidesPackage{preamble}

% Standard mathematical typesetting packages
\RequirePackage{amsfonts, amsthm, amsmath, amssymb}
\RequirePackage{mathtools}
\RequirePackage{bbm}

% Symbol and utility packages
\RequirePackage{cancel, textcomp}
\RequirePackage[mathscr]{euscript}
\RequirePackage{enumitem}
\RequirePackage[nointegrals]{wasysym}

% Extras
%\RequirePackage{tikz-cd}  % For drawing commutative diagrams easily
%\RequirePackage{quiver}
\RequirePackage{multicol, titletoc, bookmark}
\RequirePackage{microtype}  % Minature font tweaks
\RequirePackage[
  a4paper,
  left=3.5cm,
  right=3.5cm,
  top=2.5cm,
  bottom=2.5cm,
  centering,
]{geometry}
\RequirePackage{fancyhdr}
\RequirePackage{extramarks}
\RequirePackage{graphicx}
%\RequirePackage{tikz}
\RequirePackage[plain]{algorithm}
\RequirePackage{algpseudocode}
% \usetikzlibrary{automata,positioning}

\RequirePackage{silence}

\pagestyle{fancy}

\setlist[enumerate]{label={\arabic*.}}

% better left right
\let\oldleft=\left
\let\oldright=\right

\def\left{\mathopen{}\mathclose\bgroup\oldleft}
\def\right{\aftergroup\egroup\oldright}

% Sometimes helpful macros
\newcommand{\abs}[1]{\left\lvert #1 \right\rvert}
\newcommand{\norm}[1]{\left\lVert #1 \right\rVert}
\newcommand{\func}[3]{#1\colon#2\to#3}
\newcommand{\vfunc}[5]{\func{#1}{#2}{#3},\quad#4\longmapsto#5}
\newcommand{\floor}[1]{\left\lfloor#1\right\rfloor}
\newcommand{\ceil}[1]{\left\lceil#1\right\rceil}
\newcommand{\symdiff}{\mathbin{\vcenter{\hbox{\scalebox{.75}{$\triangle$}}}}}
\newcommand{\bigast}{\mathop{\scalebox{1.7}{$\ast$}}}

\renewcommand{\div}{\divisionsymbol}

% shortcuts
\def\mbb#1{\mathbb{#1}}
\def\mbbm#1{\mathbbm{#1}}
\def\mfk#1{\mathfrak{#1}}
\def\mcl#1{\mathcal{#1}}
\def\mcr#1{\mathscr{#1}}


\def\bN{\mbb{N}}
\def\bC{\mbb{C}}
\def\bR{\mbb{R}}
\def\bQ{\mbb{Q}}
\def\bZ{\mbb{Z}}
\def\Chi{\scalebox{1.25}{$\chi$}}
\newcommand*\dd{\mathop{}\!\mathrm{d}}
\newcommand{\rp}{\right)}
\newcommand{\lp}{\left(}
\newcommand{\rb}{\right]}
\newcommand{\lb}{\left[}
\newcommand{\ra}{\right\rangle}
\newcommand{\la}{\left\langle}
\newcommand{\inv}{^{-1}}
\newcommand{\eps}{\varepsilon}
\newcommand{\heps}{\frac{\eps}{2}}
\newcommand{\tmp}{}
\renewcommand{\H}[1]{%
  \renewcommand*{\tmp}[1][n]{H_{##1}\lp #1\rp}%
  \tmp
}
\renewcommand{\P}[1]{\operatorname{P}\lp #1\rp}
\newcommand{\T}{\operatorname{T}}
\newcommand{\sgn}{\operatorname{sgn}}
\newcommand{\io}{\text{ i.o.}}
\DeclareMathOperator{\img}{Img}
\renewcommand{\Re}{\mathfrak{Re}}
\renewcommand{\Im}{\mathfrak{Im}}
\newcommand{\Id}{\operatorname{Id}}
\newcommand{\cl}{\operatorname{cl}}
\newcommand{\Int}{\operatorname{Int}}
\newcommand{\Ext}{\operatorname{Ext}}
\newcommand{\skipforward}[1][0.8ex]{%
  \mbox{%
    \rlap{\(\blacktriangleright\)}%
    \hspace{#1}%
    \(\blacktriangleright\)%
  }%
}

\let\oldi\i
\let\i\infty

